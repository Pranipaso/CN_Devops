pipeline {
    agent any
    stages {
        stage("Install Dependency"){
            steps {
                sh 'npm install'
            }
        }
        stage("Run Tests"){
            steps {
                sh 'npm test'
            }
        }
    }
}